{
  "name": "token-bucket-rate-limiter",
  "version": "1.0.0",
  "description": "Token bucket rate limiter with Express middleware",
  "type": "module",
  "main": "dist/token-bucket.js",
  "types": "dist/token-bucket.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node --import tsx/esm src/server.ts",
    "dev": "tsx watch src/server.ts",
    "test": "tsx --test src/token-bucket.test.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
